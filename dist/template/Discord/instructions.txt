1. Build the docker image by running 'docker build --pull --rm -f "Dockerfile" -t dockerizediscord:latest "."'
2. Then start the container by running 'docker-compose up'
3. Go on localhost then here is it